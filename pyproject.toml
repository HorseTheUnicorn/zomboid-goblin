[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "zomboid-goblin"
version = "0.2.0"
description = "Safety-first Project Zomboid Goblin integration"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
